# For more documentation, see https://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'
# You can also refence environment variables:
#   variable=$HOME/task
#   variable=$VALUE
# Use the command 'task show' to see all defaults and overrides

news.version=2.6.0

# Files
data.location=~/.config/task
include ~/.config/task/serverrc
include dark-green-256.theme

# Datetime settings
weekstart=Monday

# Default project
default.project=inbox

# Estimate attribute
uda.estimate.type=numeric
uda.estimate.label=Estimate

# [Urgency](https://taskwarrior.org/docs/urgency/)
urgency.user.project.work.coefficient=15.0
urgency.user.tag.next.coefficient=15.0
urgency.user.tag.rnr.coefficient=10.0

# Taskwarior TUI config
# https://kdheepak.com/taskwarrior-tui/configuration/advanced/
uda.taskwarrior-tui.selection.dim=yes
uda.taskwarrior-tui.mark.indicator=
uda.taskwarrior-tui.mark-selection.indicator=x
uda.taskwarrior-tui.unmark-selection.indicator=.
uda.taskwarrior-tui.task-report.next.filter=status:pending -WAITING limit:page (+ACTIVE or +OVERDUE or due:today or scheduled:today or pri:H)

# Copied from taskwarrior source code
# https://github.com/GothenburgBitFactory/taskwarrior
# taskwarrior/src/Context.cpp#379

# Adjust reporting
report.in.columns           = id,priority,tags,description
report.in.labels            = ID,Pri,Tags,Description
report.in.description       = Inbox
report.in.filter            = status:pending limit:page (project:inbox)